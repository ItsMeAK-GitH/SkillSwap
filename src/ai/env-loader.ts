/**
 * @fileoverview This file is executed before starting the Genkit service.
 * It is responsible for loading environment variables from the `.env` file.
 */
import {config} from 'dotenv';
config();
